# Implementation Plans

Generated by the improve skill on 2026-07-27 against commit `f13f997`
(round 1). Deploy-readiness plans **006–010** planned at `afd8112` on 2026-07-27.
Plan **011** (Prisma `DATABASE_URL`) planned/executed from `12dee30`.

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Make the durable Gate 2 stack runnable and documented | P1 | M | — | DONE (code+docs; live Docker verify blocked — user not in `docker` group) |
| 002 | Validate command envelopes at the API/domain boundary | P1 | M | — | DONE (merged into `advisor/140` @ `b9b6e0f`) |
| 003 | Inspect real media bytes before marking assets ready | P1 | M | 001 | DONE (unit+probe fixtures; MinIO integration verify blocked with 001 Docker) |
| 004 | Connect admitted media into the FFmpeg preview render | P1 | L | 002, 003 | DONE |
| 005 | Keep render progress alive until a terminal phase | P1 | M | — | DONE |
| 006 | Harden local-auth and demo identity for hosted safety | P1 | S | — | DONE (branch `advisor/145-harden-local-auth` @ `9d0b88c`) |
| 007 | Provision User rows on first verified JWT | P1 | M | 006 | DONE (branch `advisor/146-jwt-user-provision` @ `5e248fe`) |
| 008 | Ship a private hosted deploy surface (API + worker + web) | P1 | L | 006, 007 | DONE (branch `advisor/147-hosted-deploy` @ `12dee30`) |
| 009 | Persist render failures and atomic media-inspect outbox | P1 | M | — | DONE (branch `advisor/148-render-fail-outbox` @ `9be6136`) |
| 010 | Bind upload size in signed PUTs and inspect Pexels like uploads | P1 | M | 009 | DONE (branch `advisor/149-media-trust-hosted` @ `9466371`) |
| 011 | Point Prisma datasource at DATABASE_URL | P1 | S | 008 | DONE (branch `advisor/150-prisma-database-url` @ `ae58872`) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (reason)

## Merge order (round 2 — not merged by advisor)

Two parallel lines from `afd8112` / `advisor/140`:

1. Auth/deploy: `145` → `146` → `147` → `150` (`…` → `12dee30` → `advisor/150-prisma-database-url`)
2. Media reliability: `148` → `149` (`9be6136` → `9466371`)

Then merge both tips into `advisor/140` (expect conflicts on
`apps/api/src/server.ts`, `apps/api/src/start.ts`, `apps/web/src/main.tsx`,
tests). Do **not** set `FMOTION_LOCAL_AUTH` on hosted after merge.

## Dependency notes

**Round 1 (001–005):** media truth + local stack — complete.

**Round 2 (006–010) — before private hosted deploy:** complete (executor
branches above; user merges).

**011:** unblocks `prisma migrate deploy` for CI and hosted by using
`env("DATABASE_URL")`.

**Still not planned (public launch):** Gate 0 legal checklist, payments, Android
Play signing, CI continuous deploy.

## Findings considered and rejected (this round)

- Exact-vs-range npm pin policy across api/worker: real but low leverage.
- Full OpenAPI `/v1` resurfacing: needed later; do not block media truth.
- Beatoven / managed FAL / payments / Flutter Web: gated or already rejected.
- CI auto-deploy: defer until images + manual deploy work (008).
- Outbox `SKIP LOCKED` multi-worker claim: follow-up after 009.
- Express stack-scrubbing error middleware + forced loopback bind: small
  follow-ups; optional fold into 008 if touching `start.ts`.
- Gate 0 / payments / Play signing: public-launch only, not private-demo blockers.
