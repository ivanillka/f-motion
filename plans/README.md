# Implementation Plans

Generated by the improve skill on 2026-07-27. Round 1 (001–005) at `f13f997`;
deploy-readiness **006–011**; export-quality **012–016**; integrate **017**;
Drafts **018**; scene audio **019**. Round 5 (direction) **020–023** planned
at `7d90804` on 2026-07-28. Round 7 (hosted safety, honest creation flow,
public/private boundary, engine extraction, and web session lifecycle)
**028–032** planned against integrated tip `f1fb25e` on 2026-07-30.
Round 8 reconciled those plans against deployed tip `c113dfe`: **028–032**
were integrated at `5b7a0e3` and the licensed-stock journey evolved through
`3cae624`/`f77b6f0`. Reliability, contract, capacity, and multi-scene plans
**033–047** were written against `c113dfe` on 2026-07-31.
Round 9 **048–050** was written on 2026-08-02 against the deployed
`advisor/133-design-contract` tip `1690eae`. It supersedes the earlier hybrid
managed-credit FAL direction: FAL is strictly BYOK, owner-scoped, explicitly
confirmed per job, and never falls back to a maintainer/platform credential.
Round 10 **051–055** was written on 2026-08-02 against product tip `6287cce`
(050 worktree). It hardens FAL job lifecycle, unblocks credential revoke,
promotes that tip over the divergent `8125787` checkout, wires final export
UX, and syncs docs/contract inventory.

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Make the durable Gate 2 stack runnable and documented | P1 | M | — | DONE (code+docs; live Docker verify blocked — user not in `docker` group) |
| 002 | Validate command envelopes at the API/domain boundary | P1 | M | — | DONE (merged into `advisor/140` @ `b9b6e0f`) |
| 003 | Inspect real media bytes before marking assets ready | P1 | M | 001 | DONE (unit+probe fixtures; MinIO integration verify blocked with 001 Docker) |
| 004 | Connect admitted media into the FFmpeg preview render | P1 | L | 002, 003 | DONE |
| 005 | Keep render progress alive until a terminal phase | P1 | M | — | DONE |
| 006 | Harden local-auth and demo identity for hosted safety | P1 | S | — | DONE (branch `advisor/145-harden-local-auth` @ `9d0b88c`) |
| 007 | Provision User rows on first verified JWT | P1 | M | 006 | DONE (branch `advisor/146-jwt-user-provision` @ `5e248fe`) |
| 008 | Ship a private hosted deploy surface (API + worker + web) | P1 | L | 006, 007 | DONE (branch `advisor/147-hosted-deploy` @ `12dee30`) |
| 009 | Persist render failures and atomic media-inspect outbox | P1 | M | — | DONE (branch `advisor/148-render-fail-outbox` @ `9be6136`) |
| 010 | Bind upload size in signed PUTs and inspect Pexels like uploads | P1 | M | 009 | DONE (branch `advisor/149-media-trust-hosted` @ `9466371`) |
| 011 | Point Prisma datasource at DATABASE_URL | P1 | S | 008 | DONE (branch `advisor/150-prisma-database-url` @ `ae58872`) |
| 012 | Honor scene focal_x/focal_y in the 9:16 cover crop | P1 | M | — | DONE (branch `advisor/012-honor-focal-cover-crop` @ `6f9290e`) |
| 013 | Harden caption burn (escape, wrap, panel, safe area) | P1 | M | — | DONE (branch `advisor/013-caption-burn-polish` @ `c255351`) |
| 014 | Persist motion presets (stop the dead control) | P2 | S–M | — | DONE (branch `advisor/014-persist-motion-presets` @ `45b4102`) |
| 015 | Add script-derived timed caption cues | P1 | L | 013 | DONE (branch `advisor/015-timed-caption-cues` @ `c44b083`) |
| 016 | Multi-scene accurate preview concat | P1 | L | 012 | DONE (branch `advisor/016-multi-scene-concat` @ `ee26d5a`) |
| 017 | Integrate round-2 + export-quality onto Gate 2 tip | P1 | M | 006–016 | DONE (branch `advisor/017-integrate-gate2-export` @ `f9ddc98`) |
| 018 | Ship server-backed Drafts (list + reopen) | P1 | M | 017 preferred | DONE (branch `advisor/018-server-drafts-list` @ `ecf0add`) |
| 019 | Honor scene audio_level in accurate preview | P2 | M | 017 preferred | DONE (branch `advisor/019-scene-audio-level` @ `f382689`) |
| 020 | Pad silent / no-audio video clips in accurate preview | P1 | S–M | — | DONE (branch `advisor/020-silent-video-audio-pad` @ `95b614a`) |
| 021 | Surface render failure with retry on web | P1 | S–M | — | DONE (branch `advisor/021-render-failure-retry` @ `96bd22d`) |
| 022 | Make conflict “Save as new project” preserve local edits | P1 | M | — | DONE (branch `advisor/022-conflict-save-as-copy` @ `2ffee1a`) |
| 023 | Add a minimal Settings step (sign-out + legal stubs) | P2 | S–M | — | DONE (branch `advisor/023-minimal-settings` @ `30503f8`) |
| 024 | Document object-storage CORS for browser uploads | P1 | S | — | DONE (branch `advisor/024-object-storage-cors-docs` @ `b866ddd`) |
| 025 | Document production same-origin `/api` proxy | P1 | S–M | 024 preferred | DONE (branch `advisor/025-hosted-api-proxy-docs` @ `5fbb584`) |
| 026 | Lengthen media-attach poll until inspect completes | P1 | S | — | DONE (branch `advisor/026-media-attach-poll` @ `1fbe4a5`) |
| 027 | Set FMOTION_ENV=hosted on Fly API config | P1 | S | 024/025 preferred | DONE (branch `advisor/027-fmotion-env-hosted` @ `7046d26`) |
| 028 | Make the hosted demo invite-only and bound render admission | P1 | M | — | DONE (`5b7a0e3`) |
| 029 | Replace the fake concept screen with a guided media-source journey | P1 | L | 028 before hosted rollout | DONE (`5b7a0e3`; auto-stock evolved at `3cae624`) |
| 030 | Establish a clean public F-Engine/private Fotium publication boundary | P1 | M | — | DONE (`5b7a0e3`) |
| 031 | Make F-Engine host-neutral and consumable as versioned packages | P1 | L | 030 | DONE (`5b7a0e3`) |
| 032 | Replace raw web tokens with Supabase PKCE session lifecycle | P1 | M | 028; after 029/031 if executed | DONE (`5b7a0e3`) |
| 033 | Restore the browser journey as a release gate | P1 | S | — | DONE (reviewed `a955957`) |
| 034 | Make Cloudflare Pages deployment topology verifiable | P1 | M | 033 | DONE (reviewed `929c042`) |
| 035 | Fail closed when hosted access configuration is missing | P1 | S | — | DONE (reviewed `1bf17d0`) |
| 036 | Freeze the exact project snapshot on every render job | P1 | M | — | DONE (reviewed `1d9a5c8` + `7e7793b`) |
| 037 | Coalesce duplicate renders for the same project revision | P1 | M | 036 | DONE (reviewed `ecabb48` + `b1461df`) |
| 038 | Seal uploaded media after quarantine inspection | P1 | M | — | DONE (reviewed `f3e0c0a` + `01e3f27`) |
| 039 | Bound and cancel untrusted media probing | P1 | M | — | DONE (reviewed `cd2dad7` + `8350b3f`) |
| 040 | Reject incomplete renders instead of producing gray video | P1 | S | 036, 038 | DONE (branch `advisor/040-reject-incomplete-render-inputs` @ `b98edb4`) |
| 041 | Hydrate scene media preview and attribution from the server | P1 | M | 033 | DONE (reviewed `98f90ad`) |
| 042 | Make the language-neutral API contract authoritative | P1 | L | 036, 037, 040, 041 | DONE (branch `advisor/042-complete-versioned-api-contract` @ `ffc39ac`; Flutter verify deferred — SDK not installed locally) |
| 043 | Stream large media transfers instead of buffering whole files | P2 | M | 038 | DONE (reviewed `aed3b76`) |
| 044 | Index and retain the transactional outbox | P2 | M | 037 | DONE (reviewed `3ed2dd0`) |
| 045 | Generate a real multi-scene licensed-stock storyboard | P1 | L | 033, 036–043 | DONE (branch `advisor/045-generate-multi-scene-stock-storyboard` @ `f6fdc49`) |
| 046 | Turn the preview into a guided multi-scene storyboard editor | P1 | L | 045 | DONE (branch `advisor/046-guided-storyboard-editor` @ `aba4294`) |
| 047 | Let users choose among three honest story concepts | P2 | M | 045 | DONE (branch `advisor/047-present-three-story-concepts` @ `4f19f03`) |
| 048 | Make FAL strictly BYOK with owner-scoped encrypted credentials | P1 | L | — | DONE (on tip @ `9461c35`; origin includes it) |
| 049 | Generate one reviewable FAL still for one scene with the user's key | P1 | L | 048 | DONE (branch `advisor/049-fal-scene-image-generation` @ `0571986`) |
| 050 | Animate one approved scene image with FAL using the owner's key | P2 | L | 048, 049 | DONE (branch `advisor/050-fal-scene-image-to-video` @ `6287cce`) |
| 051 | Harden FAL generation lifecycle (poll, inspect, cancel) | P1 | S | 049, 050 | DONE (branch `advisor/051-harden-fal-generation-lifecycle` @ `150e340`) |
| 052 | Unblock FAL credential disconnect after generation jobs exist | P1 | M | 048–050; 051 preferred | DONE (branch `advisor/052-unblock-fal-credential-disconnect` @ `40c9a7c`) |
| 053 | Promote product tip `6287cce` as the sole ship tip | P1 | M | 6287cce; 051–052 preferred | DONE (branch `advisor/053-product-tip`; gate green) |
| 054 | Wire `final` export and honest download UX | P1 | M | 053 | DONE (branch `advisor/054-wire-final-export`) |
| 055 | Sync docs and generation routes in the contract inventory | P2 | S | 053 preferred | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (reason)

## Round 10 — ship tip + FAL ops + final export (@ `6287cce`)

**Audited tip**: `6287cce` (`.worktrees/050-fal-scene-image-to-video`).
**Ship tip (round 10)**: `advisor/054-wire-final-export` (from `advisor/053-product-tip`
= `6287cce` + 051 + 052 + gate fix + final export). Worktree
`.worktrees/053-product-tip`. Full `lint`/`test`/`build` exit 0. Web e2e
**BLOCKED** here (Playwright Chromium not installed in this environment;
coverage added in `tests/e2e/web-flow.spec.ts`).
**Superseded divergent checkout**: `advisor/export-quality-merge` @ `8125787`
(merge-base `20365f9` with tip; lacks `packages/fal-host`). Do **not** execute
new plans against `8125787`. Do **not** merge its alternate history onto tip.

Recommended execution:

1. **051** — DONE.
2. **052** — DONE.
3. **053** — DONE (this ship tip).
4. **054** — DONE (web `kind: "final"` + download labeling).
5. **055** — README/getting-started honesty + generation routes in
   `route-inventory.json` / OpenAPI.

Deferred from this round (not planned): KEK multi-version vault load; Postgres
FAL confirm integration tests; YT/Reel/Story format selection; Gate 0 evidence
pack; per-export rights ZIP; `main.tsx` split.

## Round 9 — FAL BYOK only (@ `1690eae`)

Execute sequentially:

1. **048** removes managed/platform FAL paths from product truth and establishes
   encrypted owner-scoped credentials. No paid generation exists yet.
2. **049** adds one explicitly quoted and confirmed still-image job for one
   scene, with conservative at-most-one submit behavior and existing media
   quarantine/inspection before selection.
3. **050** reuses the same boundary to animate one already-approved image. It
   must not introduce text-to-video, bulk generation, or automatic attachment.

All three target the deployed branch `advisor/133-design-contract`, not the
older planning checkout. Production feature enablement remains blocked until
written FAL output ownership, commercial-use, training/data-use, and retention
evidence is approved. BYOK changes who is charged; it does not remove the host's
privacy/legal responsibility for transmitting prompts and media.

## Round 8 — reliability before multi-scene (@ `c113dfe`)

Recommended execution, respecting dependencies:

1. **033**, **035**, **036**, **038**, and **039** can start independently:
   restore the release gate, fail closed in hosted mode, and establish immutable
   render/media/process boundaries.
2. **034** follows 033. **037** follows 036. **040** follows 036+038.
   **041** follows 033. These close the concrete deployed defects.
3. **042** freezes the corrected HTTP/SSE contract before scene fields expand.
   **043** follows sealed storage; **044** follows render coalescing.
4. **045** starts only after 033 and 036–043 are integrated. It creates the
   deterministic 4–6 scene licensed-stock storyboard.
5. **046** adds the guided scene-card editor. **047** then exposes three
   concepts only after those concepts can produce genuinely different scene
   plans.

Plan 030/031 already covers the selected public F-Engine/private F-Motion host
boundary, so Round 8 deliberately does not duplicate that direction plan.

## Round 7 — safe product direction (@ `f1fb25e`)

Recommended execution:

1. **028** — close hosted admission and bound render queue growth first.
2. **029** — validate one honest upload-or-licensed-stock web journey.
3. **030** — approve license/rights and make the public/private boundary
   deny-by-default.
4. **031** — neutralize/package F-Engine only after 030's decisions.
5. **032** — adopt Supabase PKCE sessions after 028; integrate after 029/031
   when those plans touch the same web/package files.

Do not create a public remote, publish packages, enable self-service hosted
access, or add paid/generative providers while executing these plans. Those
actions require separate operator authorization and completed gates.

## Round 6 — private hosted deploy gaps (@ `ed569d1`)

1. **024** DONE — object-storage CORS docs @ `b866ddd`
2. **025** DONE — production `/api` proxy examples @ `5fbb584`
3. **026** DONE — media-attach poll @ `1fbe4a5`
4. **027** DONE — `FMOTION_ENV=hosted` @ `7046d26` (stacks 024→025→027)

Docs tip for merge: `advisor/027-fmotion-env-hosted` @ `7046d26`.
Web tip: `advisor/026-media-attach-poll` @ `1fbe4a5`.
**Merged** into `017` / `140` @ `f1fb25e`. Pushed to origin.

## Round 5 — recovery + shell (direction @ `7d90804`)

1. **020** DONE — silent/no-audio video pad (worker) @ `95b614a`
2. **021** DONE — render failure + retry (web) @ `96bd22d`
3. **022** DONE — conflict save-as-copy (web) @ `2ffee1a`
4. **023** DONE — minimal Settings (web) @ `30503f8` (stacks 021→022→023)

Web tip for merge: `advisor/023-minimal-settings` @ `30503f8`.
Worker tip: `advisor/020-silent-video-audio-pad` @ `95b614a`.
**Merged** into `017` / `140` @ `ed569d1`. Pushed to origin.

## Merge order (round 2–6 — integrated)

**Integrated** on `advisor/017-integrate-gate2-export` @ `f1fb25e`
(= `140` tip): through round 5, then **026** (web) + **024–027** (docs/Fly).
Pushed to origin.

## Dependency notes

- 001–019 historical; do not re-execute except via merges.
- Round 6 tip: `advisor/017-integrate-gate2-export` / `140` @ `f1fb25e`.
- Hosted docs must keep `FMOTION_LOCAL_AUTH` unset.
- Gate 0 still blocks Beatoven, real legal pages, and paid FAL launch claims.
- 029 depends on 028 for hosted rollout because a smoother public journey must
  not precede server-side invite enforcement.
- 031 depends on 030 because package names, license, ownership, and export paths
  must be approved before source is neutralized for publication.
- 032 depends on 028 for hosted rollout; it may be developed independently, but
  a correct login flow is not an authorization/admission policy.
- 029, 031, and 032 overlap `apps/web`/package metadata. Integrate in numeric
  order and preserve the prior plan's behavior; do not execute them as blind
  parallel branches.
- 033 is the release characterization gate for all later web work.
- 036 and 038 establish immutable render and media inputs; 037/040 depend on
  those invariants rather than adding competing object/job lifecycles.
- 042 must describe the corrected current API before 045 adds multi-scene
  fields consumed by both TypeScript and Dart.
- 043 is required before 045 because 4–6 stock assets multiply the current API
  buffering pressure. 044 is a separate capacity safeguard and may run in
  parallel after 037.
- 046/047 must preserve the public/private publication boundary established by
  030/031 and must not add host/provider details to the public engine.
- 048 is the mandatory credential/security boundary for every FAL feature. It
  removes the managed-credit/global-key branch rather than leaving it dormant.
- 049 depends on 048 because paid queue work must never precede owner-scoped
  encrypted credentials. 050 depends on 049 because it reuses the quote,
  confirmation, crash-recovery, download, inspection, and review state machine.
- 048–050 belong in the reference-host/provider boundary. They must not make
  `@f-engine/contracts` or `@f-engine/reel-engine` read secrets, call FAL, or
  choose a provider.
- 051 before 052 so disconnect is not blocked by stuck active jobs.
- 053 before 054/055 so web/docs land on the FAL-capable tip.
- 053 must not merge `8125787` wholesale; tip already has ASS/zoompan/concat.

## Findings considered and rejected

- ClipAnything / YouTube long→shorts ingest
- ML face tracking for auto-focal
- Word-level karaoke from Whisper
- Beatoven / music bed until Gate 0
- Audio ducking without a licensed bed (deferred in 019 with `ponytail:`)
- Full Home shell / bottom nav redesign (defer; Settings first)
- Server-side project clone API (YAGNI unless 022 STOPs)
- Managed FAL credits, trial credits, maintainer/platform FAL keys, and any
  silent fallback when a user has not connected their own key
- Client-only FAL key storage (async jobs require an encrypted owner-scoped
  server credential); FAL's current public APIs also do not let the app prove a
  supplied key is API rather than ADMIN scope, so the UI instructs API scope
  without falsely claiming technical rejection
- Android trash / payments / credit ledger
- Two mutable public/private forks or long-lived product branches (use public
  upstream packages plus a pinned private host)
- Publishing the existing private Git history (use a reviewed clean snapshot
  and fresh public root commit)
- Calling Pexels “open-source media” (describe it as licensed stock with
  attribution)
- Multi-scene AI planning before one real-media web journey is validated
  (still rejected; plan 045 deliberately starts deterministic and provider-neutral)
- A speculative provider-plugin framework before two real host adapters need it
- Merging divergent `8125787` history into `6287cce` to "recover" 012–016
  (those behaviors already exist on tip; the histories forked)
- Text-to-video, bulk AI generation, managed credits (still rejected)
- Splitting `apps/web/src/main.tsx` / `server.ts` before tip promotion
- KEK multi-version load and Postgres FAL confirm integration (deferred, not
  rejected — schedule after 052 if rotation is imminent)

## Plan files

001–055 as listed above; see `plans/NNN-*.md`.
